{
  "scripts": {
    "dev": "nodemon start.js",
    "deploy": "deta update -e .env && deta deploy"
  },
  "dependencies": {
    "axios": "^0.23.0",
    "deta": "^1.0.1",
    "fastify": "^3.22.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.1",
    "nodemon": "^2.0.13"
  }
}
