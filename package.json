{
  "scripts": {
    "dev": "nodemon start.js",
    "deploy": "deta update -e .env && deta deploy"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "deta": "^1.0.1",
    "fastify": "^3.25.3",
    "dotenv": "^14.1.1"
  },
  "devDependencies": {
    "eslint": "^8.7.0",
    "nodemon": "^2.0.15"
  }
}
